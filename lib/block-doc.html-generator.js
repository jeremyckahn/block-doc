'use strict';

/**
 * Generates an HTML page out of JSDoc metadata.
 * @param {Object} meta JSDoc metadata.
 * @param {string} template Path to a template directory.
 * @return {string}
 */
exports.generate = function (meta, template) {
  return '';
};
